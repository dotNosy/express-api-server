{
    "configurations": [
        {
            "name": "NODE: Attach to process",
            "type": "node",
            "request": "attach",
            "processId": "${command:PickProcess}",
            "restart": true,
            "protocol": "insepector"
        },
    ]
}